# Deprecated API Service

This directory is deprecated. The API is now served by the Next.js app in
`apps/web/app/api/*` and deployed as a single Vercel project from the repo root.

Do not deploy or run this service. It is kept only for historical reference.
