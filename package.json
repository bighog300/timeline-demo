{
  "name": "timeline-app",
  "private": true,

  "engines": {
    "node": "20.x",
    "pnpm": "9.x"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev:web": "pnpm --filter ./apps/web dev",
    "preflight": "node scripts/preflight.mjs",
    "validate:env": "node scripts/validate-env.mjs --target all",
    "validate:env:web": "node scripts/validate-env.mjs --target web",
    "smoke": "bash scripts/smoke-test.sh",
    "deploy:check": "pnpm preflight && pnpm validate:env:web && pnpm --filter ./apps/web build",
    "test": "pnpm -r --if-present test",
    "build": "pnpm --filter ./packages/shared build && pnpm --filter ./apps/web build && pnpm build:verify",
    "build:verify": "node scripts/verify-build.mjs",
    "lint": "pnpm -r --if-present lint",
    "vercel:install": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm install --frozen-lockfile",
    "vercel:build": "pnpm --filter ./apps/web build",
    "build:web": "pnpm --filter ./apps/web build"
  },
  "devDependencies": {
    "next": "^15.1.6"
  }
}
